name: .NET

trigger:
  branches:
    include: [master]

pool:
  vmImage: ubuntu-latest

stages:
  - stage: Test_Stage
    jobs:
      - job: Test
        steps:
          - task: DotNetCoreCLI@2
            inputs:
              command: 'test'

  - stage: Build_and_Lint_Stage
    jobs:
      - job: Build
        steps:
          - task: DotNetCoreCLI@2
            displayName: build
            inputs:
              command: 'build' 
      - job: Lint
        steps:
          - task: BuildQualityChecks@8
            displayName: Check build quality
            inputs:
              checkWarnings: true
              warningThreshold: '0'

  - stage: DeployStage 
    jobs:
      - job: Deploy
        steps:
        - task: Docker@2
          displayName: deploy
          inputs:
            containerRegistry: 'DockerHubConnection'
            repository: 'xgepic/weather-api'
            command: 'buildAndPush'
            Dockerfile: '**/Dockerfile'
            tags: latest